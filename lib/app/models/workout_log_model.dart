import 'package:flutter/material.dart';

class WorkoutLogModel extends ChangeNotifier {
  // Inside here we just need to call notifyListeners() any time something changes that people need to hear about
  // This should exist inside ChangeNotifierProvider
}